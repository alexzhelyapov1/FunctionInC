У буфера ввода нет EOF. Если чистим, то до '\n'.
Приоритет != выше, чем у =.
%zu - SIZE_T
char *copyA = (char *)malloc (10 * sizeof (char));


строки склеиваются хорошо, но нужно ли выделять память заранее для склейки и может ли оно затеретья?